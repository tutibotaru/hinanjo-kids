# 親子で避難所体験 - CLAUDE.md

## プロジェクト概要

小学生親子の防災体験キャンプ向けに、QR コードを読むだけで役割分担し、1ステップずつ案内に従って避難所を立ち上げる体験ができる Web アプリ。

本家プロジェクト [tutibotaru/hinanjo-app](https://github.com/tutibotaru/hinanjo-app)(自治体向け本格仕様)から派生した、**子ども親子向けの簡略・体験版**。

---

## このプロジェクトでの絶対ルール

### 設計思想(3原則)

1. **1画面に1ステップだけ**(子どもでも迷わない)
2. **「次にやることが指で押せる」**(読ませず、行動させる)
3. **判断を3択以下に**(できた / こまった / とばす)

### 想定ユーザー

- **メイン**: 小学3-6年生(8-12歳)+ 保護者の親子ペア
- **サブ**: PTA・子ども会・防災イベント主催者(運営側)
- **NOT 対象**: 自治体担当者(本家アプリを使ってもらう)

### 文体ルール(本家との最大の差分)

- **漢字は小学3-4年生レベルまで**(または平仮名)
  - 例:「設置する」→「おく」/「区画する」→「へやをわける」/「確認」→「かくにん」
- **動詞は短く・やさしく**
  - 「実施する」→「する」/「移動する」→「うごく」
- **専門用語は使わない**
  - 「フェーズ」→「ステップ」「ぶん」/「役割」→「やくわり」/「ステップ」→「やること」
- **説明は絵本トーン**
  - 「○○すると、みんながこまらないよ」「もしものとき、○○がたいせつだから」
- **句読点を「、。」と「!?」ですっきり**
- 必要な漢字は ふりがな(ルビ)候補(将来実装)

### 技術スタック

- **フロント**: Next.js 14 (App Router) + TypeScript
- **スタイル**: Tailwind CSS のみ(外部UIライブラリは使わない)
- **テーマ色**: オレンジ系(暖色・親子向け) `#F97316`
- **バックエンド**: Supabase(匿名認証・DB・リアルタイム同期)
- **配信**: PWA 対応
- **デプロイ先**: Vercel(`hinanjo-kids.vercel.app`)

### コーディング規約

- 関数コンポーネントのみ(クラスコンポーネント禁止)
- 状態管理は最初は `useState` / `useReducer`、必要になったら Zustand
- 日本語コメントOK(ただし **WHY** に限る)
- ファイル名は kebab-case、コンポーネント名は PascalCase
- import alias は `@/*`
- モバイルファースト(まず 375px 幅で美しく)

### UI の数値ルール

- ボタンの最小高さ: **48px**(「できた」緑ボタンは **52px**)
- ステップ粒度: **1〜5分** で終わる単位
- 手順表示: 最大 **3行**、各 **25文字以内**(子ども向けに少し緩和)
- ステップタイトル: **10〜20文字**(平仮名は長めOK)
- 高コントラスト、大きな文字、親指で押せるサイズ

---

## 体験キャンプ・スコープ

### 対象範囲

- **役割**: 4つだけ(うけつけ / おへや / たべもの / けがびょうき)
- **災害**: 地震のみ
- **施設**: 学校体育館のみ
- **フェーズ**: 「はじめる(15分)」+「ひらく(30分)」の2段階・全45分前後
- **言語**: 日本語のみ(漢字最小)
- **1セッション参加者**: 最大10組の親子(20-30人)
- **コンテンツ規模**: 4役割 × 7ステップ前後 = 約 28ステップ

### MVP に残す機能

1. セッション生成(QR発行)
2. QR参加 + ニックネーム入力(ログインなし)
3. 役割推薦(4問質問 + 本人選択)
4. マイミッション画面(できた / こまった / とばす)
5. 全体進捗ボード
6. 共有タイムライン(テキスト投稿のみ)
7. ⭐⭐⭐ おつかれさま画面(達成感)
8. 招待ボタン(QR・リンクで仲間を誘う)

### やってはいけないこと

- 子どもの個人情報(実名・住所・電話)を入力させること
- 大人向けの専門用語(「災害対策本部」「フェーズ」「インシデント」等)
- 漢字だらけの説明文
- 1ステップに3つ以上の動作を詰め込む
- 体験時間が1時間を超える設計
- 医療判断・法的判断を「アプリの指示」として出すこと
- マニュアルの完全再現(行動に絞る・絵本トーンで)

---

## ディレクトリ構成

```
.
├── app/                  # Next.js App Router のページ
├── components/           # UI コンポーネント
├── lib/                  # ユーティリティ、Supabase クライアント、カスタムフック
├── data/                 # ステップコンテンツ(steps.json)
├── migrations/           # Supabase の SQL(本家由来 001/004/005/006/007)
├── public/               # 静的ファイル(PWA manifest, アイコン等)
└── docs/                 # 設計ドキュメント(本家のものを継承)
```

---

## 開発フロー

- 1機能ずつブランチを切って実装
- コミットメッセージは日本語OK
- 大きな変更前にプランを共有
- **1画面ずつ作る**(一気に作らず分けて指示)
- 各画面ができたら本番(Vercel)で実機(スマホ)確認

---

## 本家との関係

- 本家 [tutibotaru/hinanjo-app](https://github.com/tutibotaru/hinanjo-app) と同じアーキテクチャ
- セキュリティ・機能改善は手動で取り込み
- コンテンツ(steps.json)・色・文言は独立した子ども向け版
- Supabase プロジェクト・Vercel プロジェクトは別

---

## ドメイン用語(子ども向けに置き換え済み)

| 本家用語 | 子ども版用語 |
|---|---|
| セッション | 体験会場 |
| 班 / 役割 | チーム / やくわり |
| ステップ | やること / ぶん |
| 進捗 | できたかどうか |
| マイミッション | じぶんがやること |
| 共有タイムライン | ひろば |
| 訓練モード | たいけんちゅう |
