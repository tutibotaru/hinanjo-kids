# hinanjo-kids 引き継ぎメモ

「親子で避難所体験」(hinanjo-kids)= 本家 `tutibotaru/hinanjo-app` から派生した
**子ども親子向けの簡略・体験版**。新セッションはこのフォルダから起動し、
**このファイルと `CLAUDE.md` を最初に読む**こと。

> このファイルは **冒頭の「★ 現時点の正」が常に最新の正**。
> 末尾の「経緯ログ」は過去の記録(当時の判断を含む。現状と食い違う場合は冒頭が優先)。

---

## ★ 現時点の正(最終更新 2026-06-06)

### イベントは開催済み
- **高梁青年会議所 福地ぼうさいキャンプ**:2026/06/06(土)〜07(日)・高梁市立福地小学校 体育館・
  親子参加・風水害想定・4班(A〜D)並行。**本番は開催ずみ**。
- 以降の作業は「**次回開催・振り返り改善**」が主目的。
- キャンプ事業そのもの(タイムテーブル/運営マニュアル)は別プロジェクト
  `C:\Users\hebik\OneDrive\デスクトップ\JC避難所体験\` で管理。混同しない。

### アプリ(本番ライブ)
- 本番 = git **`c0109f2`**(+ docs `173bc27`)→ Vercel **READY**(`hinanjo-kids.vercel.app`)。
- 内容 `data/steps.json` = **4役割 × 4ブロック = 42ステップ**。
  - 役割: **そうむ / しせつ / みずとたべもの / けんこう**(内部ID `uketsuke`/`oheya`/`monosuke`/`kyugo`)。
  - ブロック(フェーズ): **せつえい(0)→ ごはん(1)→ よる(2)→ あさ(3)**。
  - ふりがな(ルビ)付き。ミッション=ヘッダー⋯集約・進捗「このブロック◯/N」・できたサイン常時表示。
- Supabase プロジェクト `hinanjo-kids`(ref `lizysuagsjlvfokstcgi` / region ap-northeast-1)。

### ⭐ 運用モデル =「フェーズ段階開放 ＋ 中央統制」(★方針が変わった。要注意)
> **以前は phase=3 で全タスク開放=自分のペースで全部できる「A1 完全自走」だったが、
> 本番のふりかえりで取りやめ。** いまは状況に合わせてリーダーが段階的に開ける方式。

- **本番セッションは `phase=0`(せつえい)で開始**。その時点のフェーズ**以下**のタスクだけ開く
  (`mission` は `liveSession.phase` で絞り込み)。先のぶんはロックされ、勝手に進まない。
- **リーダーが各班の `/s/<コード>/manage`「つぎのフェーズ →」で進める**と、**Realtimeで全端末に
  次ブロックが即開く**(自動では進まない)。MCの合図に合わせて せつえい→ごはん→よる→あさ と開ける運用。
- フェーズが上がると参加者画面に **🔔「あたらしい やること」全画面オーバーレイ**が出て、
  一度見て下を向いた人も気づける(`components/new-phase-overlay.tsx`)。
- 現フェーズのぶんを終えると「**いまの ぶんは おしまい・つぎを まってね**」。最終(あさ)まで開いたら
  「ぜんぶ おしまい」。
- `manage` のフェーズ表示は **1始まり**(「フェーズ1/4 — せつえい」等)で状況と対応づく。
- **本番セッション JCKIDSA〜D = `family`/`training`/`phase=0`・データ0(クリーン)**。
  次回も **phase=0 で開始 ＋ リーダー進行** が前提。

### 確定した役割の実態(主催確認済=これが正。⚠捏造禁止)
- **本部** = JCの運営本部メンバー(大人)。班のホワイトボードではない。
- **ホワイトボード** = 各班の板(避難所名・人数・ルール/じょうほう/伝言)。
- **ナイトウォーク**(夜・お寺まで)準備・安全・点呼 = **総務**。道路安全は大人(JC)。子は補助(数える・合図)。
- **救護** = ぐあい悪い人を **JCの大人に伝える**(班内に救護スペースは作らない方針。ただし ky に
  「救護(休む)場所を つくる」が1つ残存=要・主催最終確認)。
- **支援物資** = 来たら **取りに行く＋数えてWB＋配る**。
- **炊飯**(ポリ袋ごはん/じゃがりこポテサラ/カレー) = 子は **配膳・片付け・説明だけ**。火/包丁/お湯/煮込み=大人。
- 資材: テープ無し(紙/ひも)・手洗い用の水無し(アルコールスプレー)・非接触体温計=各班1本・屋内テント=各班1。

### ⚠ 最重要の教訓
1. **内容を捏造しない**。実スケジュールに無い作り込み(「本部=WB」「トイレ整備」「夜のジュース」等)を
   書いて主催に叱責→削除した経緯あり。**新ステップ/活動を足す前に必ず主催の実運用を確認**。
   出典 = `JC避難所体験\documents\A_本番用_01-15\01_タイムスケジュール表…xlsx` ＋ 主催ヒアリング。
2. **push後は必ず Vercel デプロイの READY を確認**(過去に lint失敗で本番が古いまま放置の事故)。
   本番ビルド = `npm run build`(`next dev` は lint を出さない)。

### ローカル開発の注意
- `.env.local` はローカル開発も **本番 Supabase を直接参照**(キー値は応答に出さない)。
  → 検証は使い捨てセッション(`/admin/new` か `ZZ*` を SQL作成)で行い、終わったら消す。
  本番 JCKIDSA〜D は触らない。プレビュー検証は HTML/アプリ画面に対して `preview_*` ツールで実施。

---

## 環境・基本情報

| 種類 | 値 |
|---|---|
| 本番(Vercel) | https://hinanjo-kids.vercel.app |
| GitHub | https://github.com/tutibotaru/hinanjo-kids |
| Supabase | `hinanjo-kids` / ref `lizysuagsjlvfokstcgi` |
| ローカル | `C:\Users\hebik\OneDrive\デスクトップ\hinanjo-kids` |
| 接続情報 | `.env.local`(git除外) |
| 別プロジェクト(キャンプ事業) | `C:\Users\hebik\OneDrive\デスクトップ\JC避難所体験\` |
| Canva 使い方ガイド | design `DAHLOy5cVPw` / https://www.canva.com/d/8NjmXfUnDWqzmf2 (19p) |

- 本家 `hinanjo-app` とは GitHub/Supabase(本家=FMB)/Vercel/フォルダ すべて独立。改善は手動取り込み。
- 対象=親子 / 読みレベル=中学生(子はアプリ内ふりがなON)/ 災害想定=風水害。
- アプリの「リーダー」=子どもが決めるチームリーダー(そうむ uk-001)。「おとな」=保護者。別物。
- 端末は **1家族1台=1役割** 前提(進捗は役割=チーム単位で共有)。

### migration(本番DBに適用済 = 001/004/005/006/007/008/009)
- 007 anon_auth_rls(匿名認証 + per-session RLS)
- 008 kids_roles(子ども版4班IDをCHECKに追加)
- 009 camp_mode(`sessions.audience_mode` + `shared_posts.type` に reflection/stamp)
- RLS要点: `participants` の **authenticated UPDATE は `owner = auth.uid()` 必須**
  (`owner` 既定 `auth.uid()`)。→ 参加者INSERT前に匿名サインインを待つ(下記 owner競合 修正で対応済)。

### サンプルセッション(本番DBに常設・トップ画面の体験入口)
- `DEMO01`(family)「サンプル体験会場」/ `JCKIDS`(kids)「福地JC サンプル」。
- どちらも検証で**テストデータが溜まりがち**。デモ前に `/s/<コード>/manage`「ぜんぶ もとに もどす」でリセット。

---

## 主要ファイル

- **内容本体**: `data/steps.json`(42ステップ。本家とは別物)。ブロック名 `lib/phases.ts`。
- **画面**: `app/s/[code]/role/page.tsx`(役割診断4問・チーム人数ナッジ)/ `mission/page.tsx`
  (1ステップ1動作・realtimeフェーズ駆動)/ `finish/page.tsx`(おつかれ・ふりかえり)/
  `manage/page.tsx`(リーダー: フェーズ進行・一斉ストップ・班別ダッシュボード)/ `certificate`・`board`・`posts`。
- **コンポーネント**: `new-phase-overlay.tsx`(フェーズ進行通知)/ `paused-overlay.tsx`(一斉ストップ)/
  `furigana-toggle.tsx` / `ruby-text.tsx` / `invite-button.tsx`。
- **認証**: `lib/supabase/client.ts`(`ensureAnonAuth()` 匿名サインイン)。
- **配布物(印刷)**: `docs/親子ミニ手順カード.svg`、`docs/ナイトウォーク_やくそくカード.svg`、
  `docs/ポリ袋炊飯_手引きシート.svg`、`docs/避難所のおやくそくカード.svg`、
  `docs/じゆうじかんの すごしかたカード.svg`、`docs/qr/`(班別QR・掲示)、`docs/運営フロー_スタッフ用.svg`、
  `docs/物資・準備チェックリスト.md`。
- **タスク紙版(スマホ非対応者向け)**: `docs/task-sheets/`(各タスクA4 1枚 ×42 ＋ `index.html` ＋ `_all.html`)。
  生成器 `scripts/generate-task-sheets.js`(`data/steps.json` を直したら再実行で全枚更新)。
- **文章(⚠要再同期)**: `docs/避難所自主運営の進め方_親子.md`(正典)、`docs/講座進行台本_60分.md`。

---

## このセッション(2026-06-06)でやったこと = 本番反映済

| commit | 内容 |
|---|---|
| `7899f0e` | おつかれ画面の完了数を **step_id 重複排除**で最大42に(複数家族で「50/42」になっていたのを修正) |
| `73bdcfb` | **フェーズ段階開放を realtime 中央統制で機能化**(mission を `liveSession.phase` 駆動。進めた瞬間に全端末で開く)＋ **役割保存の owner競合修正**(匿名サインイン前のINSERTで `owner=NULL`→役割UPDATEが0行無言失敗→/roleループ。nickname作成前に `ensureAnonAuth()` await、役割保存も `.select()` で0行検出)＋ manageフェーズ表示を1始まりに |
| `c0109f2` | **フェーズ進行を「あたらしい やること」全画面で通知**(`new-phase-overlay.tsx`。一度見て下を向いた人が更新に気づけなかった本番の反省) |
| `173bc27` | 配布カード4枚 ＋ タスク紙版42枚＋生成スクリプト(docs/。本番アプリには無影響) |

- **設定変更(DB)**: 本番 **JCKIDSA〜D を `phase=3` → `phase=0`** に(段階開放運用へ)。全件データ0で確認済み。
- いずれもプレビュー実機相当で検証(段階開放/realtime開放/待ち表示/オーバーレイ発火・閉じ/owner非NULL/完了数)。console・サーバ エラー0・`npm run build` 成功・デプロイ READY。

---

## 残課題・改善候補(次回に向けて)

1. **doc 再同期**: `避難所自主運営の進め方_親子.md`(正典)/ `運営フロー_スタッフ用.svg`/
   `講座進行台本_60分.md` が **最新42ステップ・確定役割に未同期**(途中内容のまま)。
2. **サンプルのリセット**: `JCKIDS`(無印)はテストデータ残存。`DEMO01` も検証で汚れがち。
   デモ前に manage でリセット。
3. **new-phase オーバーレイの拡張(任意)**: いまは `mission` のみ。みんな/ひろば の別タブにいる人にも
   出すなら、同じ検知ロジックを board/posts にも(共有フック化が綺麗)。
4. **manage の進行操作のしやすさ**: 当日リーダーがフェーズを進めにくかった面があれば、現在地表示・
   ボタンの大きさ等を改善余地。班別 manage を1画面でまとめる「全体パネル」も作成可。
5. **役割内容の主催最終確認(⚠捏造禁止)**: ①みず・たべもの班は「よる」ブロックの専用ステップが0
   (ナイト中 手があく) ②けんこうの「救護へ」は当日JCの救護コーナー前提・班内救護スペース(ky)の要否。
6. **未コミット**: `docs/避難所運営アプリ_設計図.md`(今回の作成物に含めず未コミット。要否を確認)。
7. **既知のRLSアドバイザー警告**(`rls_policy_always_true` 系等)= 本家と同じ・実害なし。L3移行時対応。

---

## 次セッション開始時の推奨プロンプト(コピペ用)

```
このフォルダ(C:\Users\hebik\OneDrive\デスクトップ\hinanjo-kids)の HANDOFF.md
冒頭「★ 現時点の正」を最初に読んで現状を把握して。
アプリは本番ライブ(c0109f2 / JCKIDSA〜D=family・phase=0・42ステップ)。
運用は「フェーズ段階開放＋中央統制」=本番はphase=0開始、リーダーがmanageで進めると
realtimeで全端末に次ブロックが開き、参加者には全画面で合図が出る(A1完全自走は廃止)。
⚠ 内容は捏造しない=JCの実スケジュール＋主催の実運用が正。新ステップを足す前に主催に確認。
push後は必ずVercelデプロイのREADY確認(本番ビルド=npm run build)。
別プロジェクト(JC避難所体験)は C:\Users\hebik\OneDrive\デスクトップ\JC避難所体験\。
まず次の一手を相談したい。
```

---
---

# 経緯ログ(過去の記録・圧縮)

> 当時の判断を含む。現状と食い違う場合は冒頭「★ 現時点の正」が優先。

### Phase 1-5(2026-05-26〜27)— 派生〜JC対応の土台
- **Phase 1** 派生作成(空リポ＋手動コピー・Supabase新規・migration適用・Vercel・初回push `1488cdb`)。
- **Phase 2** ブランディング(emerald→orange・アプリ名「親子で避難所体験」・`e0ee7b7`)。
- **Phase 3** コンテンツを子ども向けに(役割7→4・文体やさしく・役割推薦4問・おつかれ画面・`330d1c6`)。
- **Phase 4** ふりがな(`lib/ruby.ts`/`ruby-text.tsx`/ON-OFFトグル)・修了証ページ・絵文字スタンプ・
  役割保存エラー修正(migration 008)。
- **Phase 5** JCキャンプ対応の汎用化: `audience_mode`(family/kids・migration 009)・共用端末「📱こうたい」・
  班別ダッシュボード(manage)・**一斉ストップ＋PausedOverlay**・ふりかえり投稿・主催団体可変(`lib/brand.ts`)・
  サンプル `JCKIDS` 常設。

### 2026-05-30〜06-01 — 運用ドキュメント＋Canva使い方ガイド
- リーダー手引き/当日チェックリスト/トラブル対応/event-setup/受付ポスター 等を整備。
- Canva デッキ `DAHLOy5cVPw` を実スクショ中心(19p)に作り直し＋進行台本納品。
- 参加者なりきりレビューで全フロー完走・エラー0を確認。ニックネーム注意書き強化・フェーズ生表示の修正。
- スクショ生成 `scripts/capture-screenshots.js`(Puppeteer・`public/screenshots/`)。

### 2026-06-02 — 役割内容を本番スケジュール準拠で全面再設計＋本番4セッション作成
- **方針**: 単一コード→**4グループ JCKIDSA/B/C/D**(班別に進捗・ボード独立)。`FKJ606` は破棄。
  audience_mode は **family** に確定。
- `data/steps.json` を **4役割×4ブロック** に再設計(39→48→45→47 と推敲)。資材・安全(火/包丁=大人)・
  場所非依存・役割均等・ふりがな・ブロック別進捗・「たすけがいる人」表現 などを反映。
- **確定した役割の実態**(本部=JCの大人/WB=班の板/ナイト=総務・道路は大人/救護=JCへ/物資=取りに行く＋数える＋配る/
  炊飯=子は説明・配膳・片付け)を主催確認で確定(→冒頭に再掲)。
- 班別QR・掲示・運営フロー・親子ミニ手順カード・60分講座台本 を整備。
- ⚠ この時点では **A1=完全自走(phase=3 全開放)** を採用していた(後にセッション5で **phase=0 段階開放** へ変更)。

### 2026-06-05 — 本番アプリ総点検＋おつかれ完了数の修正
- 参加→役割→ミッション→おつかれ→修了証→ひろば→ボード→作成 を実機相当で通し検証・エラー0。
  役割保存の旧バグ無し(008/009適用確認)。
- おつかれ「◯/42」がセッション全体の進捗行数を数えていた件を `step_id` 重複排除で修正(`7899f0e`)。

### 2026-06-06 — 本番開催 ＋ 運用モデルを「段階開放＋中央統制」へ転換
- 本番のふりかえり: phase=3 全開放だと「勝手にどんどん進んで全部埋まる」「その時点の手引きにならない」
  「フェーズを進めても各自リロードまで気づかない」。→ **A1完全自走を廃止し、上記の段階開放＋中央統制に変更**
  (`73bdcfb` realtime化＋owner競合修正、`c0109f2` 全画面通知、JCKIDSA〜D を phase=0 に)。
- 配布カード4枚＋タスク紙版42枚＋生成スクリプトを追加(`173bc27`)。
